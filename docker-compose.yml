services:
  drill-ui:
    build:
      context: .
      args:
        VITE_CLOUD_API_URL: ${VITE_CLOUD_API_URL:-http://localhost:3101}
        VITE_DEFAULT_EDGE: ${VITE_DEFAULT_EDGE:-edge5}
    expose:
      - "80"

networks:
  external:
    true
